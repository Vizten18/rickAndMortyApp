import 'package:flutter/material.dart';
import 'package:rick_and_morty_app/src/presentation/favorites/view/favorites_page.dart';

class IndexFavorites extends StatelessWidget {
  const IndexFavorites({super.key});

  @override
  Widget build(BuildContext context) {
    /// TODO: return the index page for the favorites branch. 
    /// Avoid declaring the UI here, instead, create a new widget in presentation 
    /// reference it here.
    return const FavoritesPage();
  }
}
